[package]
name = "mt2-server"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { version = "=2.8.3", features = ["unstable"] }
serde_json = "=1.0.151"

[features]
yongan = []

[profile.release]
lto = true
opt-level = "s"
